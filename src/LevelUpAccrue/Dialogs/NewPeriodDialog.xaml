<Window x:Class="LevelUpAccrue.Dialogs.NewPeriodDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新建账期"
        Width="430"
        MinHeight="310"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="新建账期" FontSize="19" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Text="选择月份" Margin="0,18,0,6" Foreground="{StaticResource MutedBrush}" />
        <DatePicker x:Name="MonthPicker" Grid.Row="2" SelectedDateFormat="Long" />
        <Border Grid.Row="3" Background="#F6F8F9" CornerRadius="5" Padding="12" Margin="0,12,0,18">
            <CheckBox x:Name="CarryForwardBox" IsChecked="True" Content="承接上一账期的累计金额" />
        </Border>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="82" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="创建" Width="82" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="Confirm_Click" />
        </StackPanel>
    </Grid>
</Window>
